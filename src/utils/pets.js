export function getPets({driver, excludePet = null}) {
  // Load the handler by driver.

  // Get all the pets.
  const pets = [];

  // Check if we need to exclude something.

  // Return the data.
  return {
    current: {},
    otherPets: {},
  };
}
